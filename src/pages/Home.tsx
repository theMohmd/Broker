import AdCardList from "components/AdCardList/AdCardList";

const Home = () => {
    return (
        <div className="container grow ">
            <AdCardList />
        </div>
    );
};

export default Home;
